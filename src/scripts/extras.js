            // const elementToMove = event.target.textContent;
                // const indexToMove = completedTaskArray.indexOf(elementToMove);
                // if (indexToMove > -1) {
                //     completedTaskArray.splice(indexToMove, 1);
                //     taskArray.push(elementToMove);
                // }
                // localStorage.setItem("taskArray", JSON.stringify(taskArray));
                // localStorage.setItem("completedTaskArray", JSON.stringify(completedTaskArray));



                // const elementToRemove = event.target.textContent;
                // const indexToRemove = taskArray.indexOf(elementToRemove);
                // if (indexToRemove > -1) {
                //     taskArray.splice(indexToRemove, 1);
                // }
                // completedTaskArray.push(li.textContent)
                // localStorage.setItem("taskArray", JSON.stringify(taskArray));
                // localStorage.setItem("completedTaskArray", JSON.stringify(completedTaskArray));
                // console.table(completedTaskArray);


                            // const elementToRemove = dragged.textContent;
            // const indexToRemove = taskArray.indexOf(elementToRemove);
            // if (indexToRemove > -1) {
            //     taskArray.splice(indexToRemove, 1);
            // }
            // localStorage.setItem("taskArray", JSON.stringify(taskArray));
            // localStorage.setItem("completedTaskArray", JSON.stringify(completedTaskArray));